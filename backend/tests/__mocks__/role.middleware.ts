export const isAdmin = (_req: any, _res: any, next: any) => next();
export const isManager = (_req: any, _res: any, next: any) => next();
export const isWorker = (_req: any, _res: any, next: any) => next();
